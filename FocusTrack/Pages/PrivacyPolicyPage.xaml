<Page x:Class="FocusTrack.Pages.PrivacyPolicyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FocusTrack.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PrivacyPolicyPage">

    <Grid Background="#333" x:Name="MainGrid" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Padding="4" Grid.Column="0" 
                Cursor="Hand" 
                Margin="10 0 0 0"
                MouseLeftButtonDown="BackIcon_MouseLeftButtonDown"
                Background="Transparent">
                <materialDesign:PackIcon Kind="ChevronLeft"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center"
                                     Width="40"
                                     Height="40"
                                     Foreground="White"
                                     IsHitTestVisible="False"/>
            </Border>

            <TextBlock Text="Privacy Policy"
                   FontSize="24"
                   Foreground="White"
                   FontWeight="Bold"
                   Margin="0"
                   Grid.Column="1"
                   VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="1" Margin="10">
            <Border 
        Background="#333"
        CornerRadius="10"
        Padding="15"
        BorderBrush="#555"
        BorderThickness="1">
                <StackPanel>

                    <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300" Padding="10">
                        <StackPanel Orientation="Vertical" Margin="5" >
                            <TextBlock 
            Text="Your privacy is very important to us. This app tracks user activity on your device to display your app usage data." 
            TextWrapping="Wrap" 
            Foreground="White" 
            FontSize="18" 
            Margin="0,0,0,20"/>

                            <TextBlock Text="• All your activity data is stored locally on your device." 
                   Foreground="White" 
                   FontSize="16" 
                   Margin="20,0,0,10"/>
                            <TextBlock Text="• We do NOT have access to your personal data." 
                   Foreground="White" 
                   FontSize="16" 
                   Margin="20,0,0,10"/>
                            <TextBlock Text="• Your usage statistics are only visible to you in the app." 
                   Foreground="White" 
                   FontSize="16" 
                   Margin="20,0,0,10"/>
                            <TextBlock Text="• No data is shared with third parties." 
                   Foreground="White" 
                   FontSize="16" 
                   Margin="20,0,0,20"/>

                            <TextBlock Text="By using this app, you agree to these terms and understand that your data remains private and secure on your device." 
                   TextWrapping="Wrap" 
                   Foreground="White" 
                   FontSize="18" 
                   Margin="0,10,0,0"/>
                        </StackPanel>
                    </ScrollViewer>

                </StackPanel>
            </Border>
        </Grid>



    </Grid>
</Page>
