<Page x:Class="FocusTrack.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FocusTrack.Pages"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:controls="clr-namespace:FocusTrack.Controls"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">

    <Grid>
        <!-- Main Content with Scroll -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Hidden"
                CanContentScroll="False"
                PanningMode="VerticalFirst">
            <Grid Margin="0,20,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Dropdown -->
                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TotalUsageTextBlock"
                        Text="04:20:45 Hours" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           Margin="50 0 0 10"
                           VerticalAlignment="Center"
                           Grid.Column="0"/>

                    <Button x:Name="RefreshButton"
                        Click="RefreshButton_Click"
                        Style="{StaticResource RefreshButton}"
                        Grid.Column="1"
                          >
                    </Button>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10 0 10 10" Grid.Column="2">
                        <ComboBox x:Name="RangeSelecter"
                              SelectionChanged="RangeSelectot_SelectionChanged"
                              Width="120">  
                            <ComboBoxItem Content="Default" Tag="today" IsSelected="True"/>
                            <ComboBoxItem Content="Last 7 Days" Tag="7d"/>
                            <ComboBoxItem Content="Last 1 Month" Tag="1m"/>
                            <ComboBoxItem Content="Last 3 Months" Tag="3m"/>
                            <ComboBoxItem Content="Over All" Tag="overall"/>
                        </ComboBox>
                    </StackPanel>

                </Grid>
                
                <!-- Chart -->
                <Grid Grid.Row="1" Margin="10 5 10 10" Height="400" VerticalAlignment="Top">
                    <lvc:CartesianChart x:Name="UsageChart" HorizontalAlignment="Stretch"/>
                </Grid>

                <!-- Date Select-->
                <Grid Grid.Row="2" Margin="0,20,0,20" HorizontalAlignment="Center">
                    <!-- Define two columns: auto width for button, remaining space for TextBlock -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Previous Day Button -->
                    <Button Style="{StaticResource PrevDayButton}" Grid.Column="0"
                Click="PrevDayButton_Click">
                    </Button>

                    <!-- Date TextBlock -->
                    <TextBlock Grid.Column="1"
                   Name="DateTextBlock"
                   Style="{StaticResource DateSelect}"
                   Text="{Binding SelectedDate, StringFormat=d-MMMM-yyyy}"
                   VerticalAlignment="Center"
                   Padding="5"
                   MouseLeftButtonUp="DateTextBlock_MouseLeftButtonUp"
                   Cursor="Hand"/>

                    <!-- Hidden DatePicker -->



                    <!-- Next Day Button -->
                    <Button Style="{StaticResource NextDayButton}" Grid.Column="2"
                Click="NextDayButton_Click">
                    </Button>

                </Grid>

                <!-- DataGrid -->

                <ItemsControl x:Name="AppUsageGrid" ItemsSource="{Binding AppUsages}" Grid.Row="3">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#222"
                                Margin="10 5"
                                Padding="1"
                                CornerRadius="5">
                                <Grid MouseLeftButtonDown="Grid_MouseLeftButtonDown"
                                Background="#222" 
                                Margin="0 5 0 0"
                                VerticalAlignment="Center"
                                Height="50"
                                Tag="{Binding}"
                                >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Image Grid.Column="0" Width="35" Height="35" Margin="20 0 0 0"
                                    Source="{Binding AppIcon, Converter={StaticResource ByteToImageConverter}}"/>
                                    
                                    <TextBlock Grid.Column="1" Text="{Binding AppName}" 
                                           Margin="20,0" 
                                           Foreground="White"
                                           VerticalAlignment="Center"
                                           FontSize="15"
                                           />
                                    <TextBlock Grid.Column="2" Text="{Binding DurationFormatted}" 
                                           Margin="20,0" 
                                           Foreground="White"
                                           VerticalAlignment="Center"
                                           FontSize="15"
                                           />


                                    <materialDesign:PackIcon Kind="ChevronRight"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Width="22"
                                        Height="22"
                                        Grid.Column="3"
                                        Foreground="White"
                                        Margin="0 0 15 0"/>

                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                
            </Grid>
            
        </ScrollViewer>


        <!-- Loader overlay -->
        <Border x:Name="LoaderOverlay"
            Background="#80000000"
            Visibility="Collapsed"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">

                <!-- Spinner -->
                <Canvas Width="80" Height="80">
                    <Ellipse Width="60" Height="60" Stroke="DodgerBlue" StrokeThickness="6" StrokeDashArray="2 4">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" CenterX="30" CenterY="30"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Canvas.Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                 Storyboard.TargetProperty="Angle"
                                                 From="0" To="360" Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Canvas>

                <!--
                <TextBlock Text="Loading..."
                   Foreground="White"
                   FontSize="16"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,90,0,0"/>
                -->
            </Grid>
        </Border>



        <Popup x:Name="CalendarPopup"
          Placement="Center"
          StaysOpen="False"
          AllowsTransparency="True"
          PopupAnimation="Fade"
          IsOpen="{Binding IsCalendarOpen}">
            <Border Background="#222" CornerRadius="8" Padding="10">
                <controls:CustomCalendar x:Name="CustomCalendarName" SelectedDate="{Binding SelectedDate, Mode=TwoWay}" />
            </Border>
        </Popup>




    </Grid>
</Page>