<Page x:Class="FocusTrack.Pages.Settings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FocusTrack.Pages"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:controls="clr-namespace:FocusTrack.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Settings">

    <Grid Background="#333" x:Name="MainGrid" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Padding="4" Grid.Column="0" 
                    Cursor="Hand" 
                    Margin="10 0 0 0"
                    MouseLeftButtonDown="BackIcon_MouseLeftButtonDown"
                    Background="Transparent">
                <materialDesign:PackIcon Kind="ChevronLeft"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Center"
                                         Width="40"
                                         Height="40"
                                         Foreground="White"
                                         IsHitTestVisible="False"/>
            </Border>

            <TextBlock Text="Settings"
                       FontSize="24"
                       Foreground="White"
                       FontWeight="Bold"
                       Margin="0"
                       Grid.Column="1"
                       VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border
                    Grid.Row="0"
                    CornerRadius="10"
                    Background="#222"
                    Padding="10, 0"
                    Margin="0,5">

                <Grid
                      Height="50"
                      VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Track Private Browser"
                               Foreground="White"
                               FontSize="16"
                               VerticalAlignment="Center"
                               FontWeight="Bold"/>

                    <ToggleButton x:Name="TrackPrivateModeToggle"
                                  Grid.Column="1"
                                  Margin="0 0 10 0"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Right"
                                  Style="{StaticResource CustomSliderToggleButton}" 
                                  Checked="TrackPrivateModeToggle_Changed"
                                  Unchecked="TrackPrivateModeToggle_Changed"/>

                </Grid>
            </Border>

            <Border
                 Grid.Row="1"
                 CornerRadius="10"
                 Background="#222"
                 Padding="10, 0"
                 Margin="0, 5">

                <Grid
                   Height="50"
                   VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                        Text="Track VPN's"
                        Foreground="White"
                        FontSize="16"
                        VerticalAlignment="Center"
                        FontWeight="Bold"/>

                    <ToggleButton x:Name="TrackVPNToggle"
                       Grid.Column="1"
                       Margin="0 0 10 0"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Style="{StaticResource CustomSliderToggleButton}" 
                       Checked="TrackVPNToggle_Changed"
                       Unchecked="TrackVPNToggle_Changed"/>

                </Grid>

            </Border>

            <Border
                 Grid.Row="2"
                 CornerRadius="10"
                 Background="#222"
                 Padding="10, 0"
                 Margin="0, 5">

                <StackPanel>
                    <!-- Clickable Header Grid -->
                    <Grid Height="50" VerticalAlignment="Center" MouseLeftButtonDown="FocauseModeGrid_Click">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                           Text="Focause Mode"
                           Foreground="White"
                           FontSize="16"
                           VerticalAlignment="Center"
                           FontWeight="Bold"/>

                        <materialDesign:PackIcon x:Name="ChevronIcon"
                                 Kind="ChevronDown"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 Width="22"
                                 Height="22"
                                 Grid.Column="1"
                                 Foreground="White"
                                 Margin="0 0 15 0"/>
                    
                    
                    </Grid>

                    <!-- Collapsible Content Grid -->
                    <Grid x:Name="DetailsGrid" Visibility="Collapsed" Background="#222" Margin="0 10 0 0" Height="Auto">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Grid Height="50" VerticalAlignment="Center" Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Title -->
                            <TextBlock Grid.Column="0"
                               Text="Select a time — you’ll be notified when this time is reached."
                               Foreground="White"
                               FontSize="16"
                               VerticalAlignment="Center"
                               FontWeight="Normal"/>

                            <!-- Time Display -->
                            <Border Grid.Column="1"
                                CornerRadius="5"
                                Padding="10"
                                Background="#333"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Right"
                                MouseLeftButtonDown="TimeBorder_Click">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock x:Name="HourText" Text="00" Foreground="White" FontSize="18" Margin="10,0"/>
                                    <Border BorderBrush="Gray" BorderThickness="1" Height="20" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="MinuteText" Text="00" Foreground="White" FontSize="18" Margin="10,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Popup -->
                            <Popup x:Name="TimePopup"
                                   PlacementTarget="{Binding ElementName=HourText}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   PopupAnimation="Slide" 
                                   Closed="TimePopup_Closed"
                                   >
                                <Border Background="#444" Padding="10" CornerRadius="5" 
                                        BorderThickness="1"
                                        BorderBrush="#222"
                                   Margin="0 0 25 0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Hours -->
                                        <Grid Grid.Column="0" >
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <materialDesign:PackIcon x:Name="HourUpIcon"
                                                                     MouseLeftButtonDown="HourUpIcon_Click"
                                                                     Grid.Row="0"
                                                                     Grid.Column="1"
                                                                     Kind="ChevronUp"
                                                                     Width="22"
                                                                     Height="22"
                                                                     Foreground="White"
                                                                     Cursor="Hand"
                                                                     VerticalAlignment="Center"
                                                                     HorizontalAlignment="Center"
                                                                     Margin="0"/>
                                            <TextBlock Grid.Row="1"
                                                        Text="Hour"
                                                       Foreground="FloralWhite"
                                                       HorizontalAlignment="Center"
                                                       Margin="10"
                                                       FontSize="18"
                                                       FontWeight="Bold"/>

                                            <ListBox x:Name="HourList" 
                                                 Grid.Row="2"
                                                 FontSize="17"
                                                 Width="50" 
                                                 Margin="0 0 10 0"
                                                 Height="200" 
                                                 Background="Transparent" 
                                                 Foreground="White"
                                                 SelectionChanged="Hour_SelectionChanged"
                                                 BorderThickness="0"
                                                 ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                                 ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                                 HorizontalContentAlignment="Center"
                                                 VerticalContentAlignment="Center"/>


                                            <materialDesign:PackIcon    x:Name="HourDownIcon"
                                                                        MouseLeftButtonDown="HourDownIcon_Click"
                                                                        Grid.Row="3"
                                                                        Kind="ChevronDown"
                                                                        VerticalAlignment="Center"
                                                                        HorizontalAlignment="Center"
                                                                        Width="22"
                                                                        Height="22"
                                                                        Grid.Column="1"
                                                                        Foreground="White"
                                                                        Cursor="Hand"/>

                                        </Grid>

                                        <!-- Miniuts -->
                                        <Grid Grid.Column="1" >
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <materialDesign:PackIcon x:Name="MiniutUpIcon"
                                                                     MouseLeftButtonDown="MiniutUpIcon_Click"
                                                                    Grid.Row="0"
                                                                    Kind="ChevronUp"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Center"
                                                                    Width="22"
                                                                    Height="22"
                                                                    Grid.Column="1"
                                                                    Foreground="White"
                                                                    Cursor="Hand"/>

                                            <TextBlock Grid.Row="1"
                                              Text="Minutes"
                                             Foreground="FloralWhite"
                                             HorizontalAlignment="Center"
                                             Margin="10"
                                             FontSize="18"
                                             FontWeight="Bold"/>


                                            <ListBox x:Name="MinuteList" 
                                                    Grid.Row="2" 
                                                    FontSize="17"
                                                    Width="50" 
                                                    Margin="0 0 10 0"
                                                    Height="200" 
                                                    Background="Transparent"
                                                    Foreground="White" 
                                                    BorderThickness="0"
                                                    SelectionChanged="Minute_SelectionChanged"
                                                    ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                                    HorizontalContentAlignment="Center"
                                                    VerticalContentAlignment="Center"/>

                                            <materialDesign:PackIcon x:Name="MiniutDownIcon"
                                                                     MouseLeftButtonDown="MiniutDownIcon_Click"
                                                                    Grid.Row="3"
                                                                    Kind="ChevronDown"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Center"
                                                                    Width="22"
                                                                    Height="22"
                                                                    Grid.Column="1"
                                                                    Foreground="White"
                                                                    Cursor="Hand"/>

                                        </Grid>
                                    </Grid>
                                </Border>
                            </Popup>
 
                        </Grid>

                        <Grid Grid.Row="1" Margin="0, 10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Title -->
                            <TextBlock Grid.Column="0"
                               Text="Notified every time whane this time is reached."
                               Foreground="White"
                               FontSize="16"
                               VerticalAlignment="Center"
                               FontWeight="Normal"/>

                            <ToggleButton x:Name="NotifyEveryTime"
                               Grid.Column="1"
                               Margin="0 0 10 0"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               Style="{StaticResource CustomSliderToggleButton}" 
                               Checked="NotifyEveryTime_Changed"
                               Unchecked="NotifyEveryTime_Changed"/>

                        </Grid>

                    </Grid>
                
                    
                </StackPanel>


            </Border>

            <!-- Active Traking Scope -->
          <!--
            <Border
               Grid.Row="3"
               CornerRadius="10"
               Background="#222"
               Padding="10, 0"
               Margin="0, 5">

                <Grid
                     Height="50"
                     VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                      Text="Activity Tracking Scope"
                      Foreground="White"
                      FontSize="16"
                      VerticalAlignment="Center"
                      FontWeight="Bold"/>


                    <Border x:Name="ActivityTrackingScopeButton" 
                            Grid.Column="1"
                         CornerRadius="5"
                         Padding="10"
                         Background="#333"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Right"
                         MouseLeftButtonDown="ActivityTrackingScope_Click"
                            >
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock x:Name="ActivityTrackingScope" Text="Active Only" Foreground="White" FontSize="18" Margin="10,0"/>
                        </StackPanel>
                    </Border>


                    <Popup x:Name="ActivityTrackingScopePopup"
                       PlacementTarget="{Binding ElementName=ActivityTrackingScopeButton}"
                       Placement="Bottom"
                       AllowsTransparency="True"
                       PopupAnimation="Slide" 
                       Closed="ActivityTrackingScopePopup_Closed"
                       >

                        <Grid>
                            <ListBox x:Name="ActivityTrackingScopeListBox"
                                    Width="Auto"
                                    Background="#333"
                                    Foreground="White"
                                    VerticalContentAlignment="Center"
                                     FontSize="16">
                                <ListBoxItem Content="Active Apps Only" />
                                <ListBoxItem Content="Entire Screen"
                                             Margin="0 10 0 0"/>
                            </ListBox>
                        </Grid>
                    </Popup>
                    
                </Grid>
            </Border>

        -->




    </Grid>



        <!-- Add this transparent overlay grid for popup alerts -->
        <Popup x:Name="PrivateModeAlertPopup"
           Placement="Center"
           AllowsTransparency="True"
           StaysOpen="False"
           PopupAnimation="Fade"
           HorizontalAlignment="Center"
           VerticalAlignment="Center">

            <!-- Remove fixed width from Border so content can size naturally -->
            <Border Background="#222" CornerRadius="12" Padding="25" MinWidth="350" MinHeight="220">
                <controls:PrivateModeAlert x:Name="PrivateModeAlertControl"/>
            </Border>
        </Popup>

        <Popup x:Name="VpnAlertPopup"
               Placement="Center"
               AllowsTransparency="True"
               StaysOpen="False"
               PopupAnimation="Fade"
               HorizontalAlignment="Center"
               VerticalAlignment="Center">
            <Border Background="#222"
                    CornerRadius="12"
                    Padding="25"
                    MinWidth="350"
                    MinHeight="220">
                <controls:VpnAlert x:Name="VpnAlertControl"/>
            </Border>
        </Popup>
    


    </Grid>
</Page>
